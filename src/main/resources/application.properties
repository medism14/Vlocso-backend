spring.application.name=backend
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mamiotban@gmail.com
spring.mail.password=nksp cpfo veoa zyig
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

spring.http.encoding.enabled=true
spring.http.encoding.charset=UTF-8
spring.http.encoding.force=true

# URL de la base de données en ligne
spring.datasource.url=jdbc:postgresql://dpg-csu8sl5umphs738o2glg-a.frankfurt-postgres.render.com/vlocso
spring.datasource.username=vlocso_user
spring.datasource.password=LR7SpkC59JofFupmPySV20K6Gs99sq8Y

# URL pour fastapi en ligne
# fastapi.url=https://vlocso-ai-1.onrender.com/recommend/

# URL de la base de données en local
# spring.datasource.url=jdbc:postgresql://localhost:5432/vlocso
# spring.datasource.username=postgres
# spring.datasource.password=momima456

# URL pour fastapi en local
fastapi.url=http://localhost:8000/recommend/

# Driver class name
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuration JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Activer la journalisation SQL pour le débogage (optionnel)
spring.jpa.show-sql=true

# Configuration du pool de connexions
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=1200000

# Configuration des transactions
spring.transaction.default-timeout=300
spring.jpa.properties.hibernate.jdbc.batch_size=50
spring.jpa.properties.hibernate.order_inserts=true
spring.jpa.properties.hibernate.order_updates=true
spring.jpa.properties.hibernate.jdbc.batch_versioned_data=true

# Configuration de la base de données
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# Augmenter les timeouts
spring.mvc.async.request-timeout=3600000
server.tomcat.connection-timeout=3600000

# Désactiver l'ouverture de session JDBC LOB non contextuelle
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Swagger UI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method

server.port=${PORT:8080}

# Configuration pour le health check
management.endpoint.health.enabled=true
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Désactiver les logs SQL en production
logging.level.org.hibernate.SQL=INFO
logging.level.org.hibernate.type.descriptor.sql=INFO